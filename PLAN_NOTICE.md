# 공지 기획서 (공지 등록 → 카카오톡 전달)

집회마다 공지를 여러 건 등록하고, 신청 웹에 띄우고, 신청자에게 카카오톡으로 전달한다.
기존 집회 설정의 **안내 문구**(`gatherings.notice`)는 "집회 소개에 늘 붙어 있는 한 덩어리"라 그대로 둔다.
공지는 그와 달리 **시간순으로 쌓이고, 보낸 기록이 남는 것**이다.

```
[운영자] 공지 작성 ──▶ 신청 웹 공지 목록 (누구나)
              │
              └─ 보내기: 대상 고르기 → 메시지 미리보기 → [복사] / [번호 복사] / [알림톡]
                                                              └─ 보낸 기록(notice_sends)
```

---

## 1. 카카오톡으로 "보낸다"는 게 실제로 뭔가

여기가 이 기획의 전부다. 먼저 못 하는 것부터 적는다.

**전화번호만 알면 카카오톡을 보낼 수 있는 API 는 없다.** [높은 확신]
카카오에서 전화번호 기준으로 메시지를 넣을 수 있는 통로는 **알림톡(비즈메시지)** 하나뿐이고, 그건
① 카카오톡 채널 + 비즈니스 인증, ② 발송 대행사(중계사) 계약, ③ **템플릿 사전 심사·승인**,
④ 건당 과금이 전제다. 승인된 템플릿의 변수 자리만 채워 보낼 수 있어서 **자유 문구 공지는 그대로 못 나간다.**
그리고 비밀키가 필요하니 **브라우저에서 직접 부를 수 없다** — 서버가 있어야 한다.

> 요금·심사 기준·대행사 목록은 바뀐다. 계약 전에 카카오 비즈메시지 안내와 대행사 자료를 직접 확인하세요.
> (이 문서는 금액·소요일을 적지 않는다 — 확인되지 않은 숫자를 적어두면 그게 기준이 되어버린다.)

카카오 JS SDK 의 **공유하기**(`Kakao.Share`)는 키 없이도 되지만, **보낼 채팅방을 사람이 고른다.** [높은 확신]
"확정된 32명에게 각각" 같은 건 안 된다. 게다가 이 앱의 운영자 화면은 macOS·Windows 데스크톱이 주 무대라
JS SDK 가 아예 없는 환경이다.

### 그래서 두 갈래로 간다

| | 무엇 | 설정 | 언제 쓰나 |
|---|---|---|---|
| **A. 복사해서 붙여넣기** | 공지를 카톡 메시지 모양으로 만들어 클립보드에 넣는다. 운영자가 단톡방에 붙여넣는다. | **없음. 지금 바로 된다** | 평소. 집회는 대개 단톡방이 이미 있다 |
| **B. 알림톡** | Edge Function 이 대행사 API 로 개별 발송 | 채널·대행사·템플릿 승인 필요 | 단톡방에 없는 사람까지 개별로 닿아야 할 때 |

A 를 **주 경로**로 만든다. 설정이 0이고, 실패할 구석이 없고, 운영자가 보내기 전에 눈으로 한 번 더 본다.
B 는 **껍데기까지만** 만들어 둔다 (§5) — 대행사가 정해지면 함수 한 곳만 고치면 되게.

**번호 복사**도 같이 둔다. 대상자 휴대폰 번호를 줄바꿈으로 복사해 두면 단톡방 초대나 문자 발송에 그대로 쓴다.

---

## 2. 데이터

```sql
notices        id, gathering_id, title, body, pinned, published, created_at, updated_at
notice_sends   id, notice_id, channel, target, count, sent_at, sent_by
```

- `published` — 끄면 신청 웹에 안 보인다. 미리 써 두고 때가 되면 켠다.
- `pinned` — 신청 웹 목록 맨 위에 고정. 최신순 정렬보다 먼저 본다.
- `notice_sends.channel` — `copy`(메시지 복사) · `phones`(번호 복사) · `alimtalk`.
  복사도 기록한다. 실제 발송은 사람 손이지만, **"이 공지를 언제 누구한테 돌렸나"가 운영 중 제일 자주 묻는 것**이다.
  화면에는 "보냄"이 아니라 `channel` 그대로 "메시지 복사 · 입금확인 32명"으로 적어 과장하지 않는다.
- `target` — `all`(취소 제외 전체) · `confirmed`(입금확인) · `pending`(입금대기).

권한(RLS)은 기존 규칙을 그대로 따른다.

- `notices` — **공개된 공지는 누구나 읽기**(`published or is_admin()`), 쓰기는 운영자만.
- `notice_sends` — 운영자만. 신청자에게 보일 이유가 없다.

신청자는 `registrations` 에 직접 못 닿으므로, 대상자 번호를 뽑는 일은 **운영자 앱에서 이미 불러온 신청 목록**으로 한다.
서버에 새 조회 함수를 만들지 않는다.

---

## 3. 메시지 모양

`gathering.dart` 의 `noticeMessage()` 하나가 만든다. **회비 계산(`quote()`)과 같은 원칙 — 계산하는 곳이 둘이면 어긋난다.**

```
[가을수련회] 준비물 안내

세면도구와 성경책을 챙겨 오세요.
주차는 본관 뒤 주차장을 이용하시면 됩니다.

▶ 신청 · 조회: https://yjchae.github.io/room-assignment/?g=<집회id>
```

- 제목 앞의 `[집회 이름]` — 단톡방에 여러 집회 공지가 섞여도 구분된다.
- 맨 아래 신청 링크 — 이미 `config.dart` 의 `applyLink()` 가 있다. 공지를 받은 사람이 바로 자기 신청을 확인한다.
- 카카오톡에 붙여넣는 순수 텍스트다. 마크다운·이모지 장식을 넣지 않는다 (붙여넣으면 깨진다).

---

## 4. 화면

### 4.1 운영자 — 왼쪽 메뉴에 **[알림 → 공지]** 한 칸 추가

`main.dart` 의 `_dest` 마지막(7번)에 붙이고 `noticesTab` 상수로 부른다.
기존 탭 번호(`settingsTab` 0 · `registrationsTab` 1 · `assignTab` 4)는 건드리지 않는다.

좌우 2단 — 방배정·자동배정 화면과 같은 골격(room-ui §5).

- **좌 (320)**: 공지 목록. 고정 공지가 위, 그다음 최신순. 미공개는 흐리게 + "비공개" 표시. 맨 위 [새 공지].
- **우**: 고른 공지의 제목·본문 편집 + [공개] [고정] 스위치 + [저장] [삭제],
  그 아래 **보내기** 카드 — 대상 칩(전체 / 입금확인 / 입금대기) · 메시지 미리보기 · 버튼 · 보낸 기록.

대상 인원수는 칩에 같이 적는다("입금확인 32"). 0명이면 버튼은 숨기지 않고 비활성(room-ui §6).

### 4.2 신청 웹 — 집회 페이지 위쪽에 공지 카드

집회를 불러올 때 공지도 같이 불러온다(`Future` 하나로 묶어 로딩이 두 번 깜빡이지 않게).
공개된 공지만 온다(RLS 가 거른다 — 앱에서 또 거르지 않는다).

- 고정 공지 먼저, 그다음 최신순. 3건까지 펼쳐 두고 나머지는 [공지 N개 더 보기].
- 본문이 길면 접어 두고 누르면 펼친다. 공지가 없으면 카드 자체가 안 나온다.

---

## 5. 알림톡 (선택, 껍데기까지)

`supabase/functions/send-notice-kakao/index.ts` — Supabase Edge Function.

```
운영자 앱 ──notice_id, target, 대상 번호──▶ Edge Function ──▶ (대행사 API) ──▶ 카카오
                                              └─ notice_sends 기록
```

Edge Function 이 하는 일: 운영자인지 확인 → 환경변수 확인 → 대행사로 중계 → 기록.
**대행사 API 의 요청 모양은 회사마다 다르고 이 문서가 단정할 수 없다.** 그래서 함수는 공급사 중립으로 둔다 —
`ALIMTALK_ENDPOINT` 로 정해진 JSON 을 POST 하고, 그 주소를 대행사에 맞추는 건 계약 후에 한다.

| 환경변수 | 뜻 |
|---|---|
| `ALIMTALK_ENDPOINT` | 중계할 주소 (대행사 API 또는 그 앞에 둔 내 어댑터) |
| `ALIMTALK_TOKEN` | 그 주소에 붙일 `Authorization` 헤더 값 |
| `ALIMTALK_TEMPLATE` | 승인받은 템플릿 코드 |

셋 중 하나라도 비어 있으면 `NOT_CONFIGURED` 를 돌려주고, 앱은
"알림톡이 아직 설정되지 않았습니다. [메시지 복사]로 단톡방에 보내세요."로 안내한다.
**설정 전에도 앱이 멀쩡히 돌아가는 게 요점이다.**

배포는 CI 에 넣지 않는다(`supabase functions deploy` 는 별도 토큰이 필요하고, 쓰지도 않는 함수를 매 push 마다 올릴 이유가 없다).
쓰게 되는 날 `supabase functions deploy send-notice-kakao` 를 한 번 돌린다.

---

## 6. 일부러 안 한 것 (`ponytail:`)

- **예약 발송** — 보낼 시각을 정해두는 것. 서버에 스케줄러(pg_cron 등)가 필요하고, 지금은 공지를 쓰는 사람이
  곧 보내는 사람이라 값을 못 한다. 필요해지면 `notices` 에 `send_at` 을 붙이고 Edge Function 을 시간마다 깨운다.
- **읽음 확인** — 카카오톡이 알려주지 않는다. 링크에 추적 파라미터를 붙이는 방법이 있지만 신청자를 추적하는 건
  이 도구의 성격이 아니다.
- **개인별 문구 치환**("○○님, 302호입니다") — 알림톡 템플릿이 정해진 뒤에 의미가 있다. A 경로(단톡방)에서는 쓸 데가 없다.
- **공지 첨부 이미지** — 포스터 한 장이면 충분하고, 카카오톡에 붙여넣을 땐 어차피 텍스트만 간다.

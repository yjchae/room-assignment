# 공지 기획서 (공지 등록 → 카카오톡 전달)

집회마다 공지를 여러 건 등록하고, 신청 웹에 띄우고, 신청자에게 카카오톡으로 전달한다.
기존 집회 설정의 **안내 문구**(`gatherings.notice`)는 "집회 소개에 늘 붙어 있는 한 덩어리"라 그대로 둔다.
공지는 그와 달리 **시간순으로 쌓이고, 돌린 기록이 남는 것**이다.

```
[운영자] 공지 작성 ──▶ 신청 웹 공지 목록 (누구나)
              │
              └─ 보내기: 대상 고르기 → 메시지 미리보기 → [메시지 복사] / [번호 복사] / [공지 링크 복사]
                                                              └─ 돌린 기록(notice_sends)
```

**돈이 드는 통로는 쓰지 않는다.** 이 문서의 모든 기능은 Supabase 무료 요금제와 GitHub Pages
안에서 끝난다. 어떤 선택지가 왜 유료인지는 §1, 무료로 더 갈 수 있는 길(브라우저 푸시)은 §5 에 있다.

---

## 1. 카카오톡으로 "보낸다"는 게 실제로 뭔가

여기가 이 기획의 전부다. 먼저 못 하는 것부터 적는다.

**전화번호만 알면 카카오톡을 보낼 수 있는 API 는 없다.** [높은 확신]
카카오에서 전화번호 기준으로 메시지를 넣을 수 있는 통로는 **알림톡(비즈메시지)** 하나뿐이고, 그건
① 카카오톡 채널 + 비즈니스 인증, ② 발송 대행사(중계사) 계약, ③ **템플릿 사전 심사·승인**,
④ **건당 과금**이 전제다. 승인된 템플릿의 변수 자리만 채워 보낼 수 있어서 **자유 문구 공지는 그대로 못 나간다.**
그리고 비밀키가 필요하니 브라우저에서 부를 수 없어 서버까지 있어야 한다.

> 계약·심사·요금 조건은 바뀐다. 쓰기로 한다면 카카오 비즈메시지 안내와 대행사 자료를 직접 확인하세요.
> (이 문서는 금액·소요일을 적지 않는다 — 확인되지 않은 숫자를 적어두면 그게 기준이 되어버린다.)

**→ 유료라서 안 쓴다.** 집회 하나에 수백 건씩, 집회마다 반복되는 비용이다. 단톡방이 이미 있는 판에
그 돈을 낼 이유가 없다.

카카오 JS SDK 의 **공유하기**(`Kakao.Share`)는 돈이 안 들지만, **보낼 채팅방을 사람이 고른다.** [높은 확신]
"확정된 32명에게 각각" 같은 건 안 된다. 결국 사람이 채팅방을 고르는 건 복사·붙여넣기와 같은데,
카카오 개발자 앱 등록·JavaScript 키·도메인 등록이 더 붙고 데스크톱 운영자 앱에서는 아예 안 된다.
**→ 얻는 게 없어서 안 쓴다.**

### 그래서 하는 일

운영자가 **복사해서 단톡방에 붙여넣는다.** 앱이 하는 일은 "붙여넣기 좋은 모양으로 만들어 주는 것"이다.

| 버튼 | 무엇을 복사하나 | 언제 |
|---|---|---|
| **메시지 복사** | 제목 + 본문 + 링크, 카카오톡에 그대로 붙는 순수 텍스트 | 평소. 공지는 대개 이거 하나면 끝난다 |
| **번호 복사** | 대상자 휴대폰 번호를 줄바꿈으로 | 단톡방에 초대할 때 |
| **공지 링크 복사** | 그 공지가 펼쳐진 채 열리는 신청 웹 주소 | 공지가 길거나, 나중에 다시 볼 링크가 필요할 때 |

설정이 0이고, 실패할 구석이 없고, 운영자가 보내기 전에 눈으로 한 번 더 본다.

---

## 2. 데이터

```sql
notices        id, gathering_id, title, body, pinned, published, created_at, updated_at
notice_sends   id, notice_id, channel, target, count, sent_at, sent_by
```

- `published` — 끄면 신청 웹에 안 보인다. 미리 써 두고 때가 되면 켠다.
- `pinned` — 신청 웹 목록 맨 위에 고정. 최신순 정렬보다 먼저 본다.
- `notice_sends.channel` — `copy` · `phones` · `link`. 셋 다 "복사"다.
  복사도 기록한다. 실제 발송은 사람 손이지만 **"이 공지를 언제 누구한테 돌렸나"가 운영 중 제일 자주 묻는 것**이다.
  화면에는 "보냄"이 아니라 `channel` 그대로 "메시지 복사 · 입금확인 32명"으로 적어 과장하지 않는다.
- `target` — `all`(취소 제외 전체) · `confirmed`(입금확인) · `pending`(입금대기).

권한(RLS)은 기존 규칙을 그대로 따른다.

- `notices` — **공개된 공지는 누구나 읽기**(`published or is_admin()`), 쓰기는 운영자만.
- `notice_sends` — 운영자만. 신청자에게 보일 이유가 없다.

신청자는 `registrations` 에 직접 못 닿으므로, 대상자 번호를 뽑는 일은 **운영자 앱에서 이미 불러온 신청 목록**으로 한다.
서버에 새 조회 함수를 만들지 않는다.

---

## 3. 메시지 모양

`gathering.dart` 의 `noticeMessage()` 하나가 만든다. **회비 계산(`quote()`)과 같은 원칙 — 계산하는 곳이 둘이면 어긋난다.**

```
[가을수련회] 준비물 안내

세면도구와 성경책을 챙겨 오세요.
주차는 본관 뒤 주차장을 이용하시면 됩니다.

▶ 자세히 보기 · 신청: https://yjchae.github.io/room-assignment/?g=<집회id>&n=<공지id>
```

- 제목 앞의 `[집회 이름]` — 단톡방에 여러 집회 공지가 섞여도 구분된다.
- 맨 아래 링크(`noticeLink()`) — 누르면 그 공지가 펼쳐진 채로 집회 페이지가 열리고, 거기서 바로 신청·조회도 된다.
  아직 저장 안 한 공지는 붙일 id 가 없어 집회 페이지 주소 그대로 나간다.
- 카카오톡에 붙여넣는 순수 텍스트다. 마크다운·이모지 장식을 넣지 않는다 (붙여넣으면 깨진다).

---

## 4. 화면

### 4.1 운영자 — 왼쪽 메뉴에 **[알림 → 공지]** 한 칸 추가

`main.dart` 의 `_dest` 마지막(7번)에 붙이고 `noticesTab` 상수로 부른다.
기존 탭 번호(`settingsTab` 0 · `registrationsTab` 1 · `assignTab` 4)는 건드리지 않는다.

좌우 2단 — 방배정·자동배정 화면과 같은 골격(room-ui §5).

- **좌 (320)**: 공지 목록. 고정 공지가 위, 그다음 최신순. 미공개는 흐리게 + "비공개" 표시. 맨 위 [새 공지].
- **우**: 고른 공지의 제목·본문 편집 + [공개] [고정] 스위치 + [저장] [삭제],
  그 아래 **보내기** 카드 — 대상 칩(전체 / 입금확인 / 입금대기) · 메시지 미리보기 · 복사 버튼 3개 · 돌린 기록.

대상 인원수는 칩에 같이 적는다("입금확인 32"). 0명이면 버튼은 숨기지 않고 비활성(room-ui §6).
저장 전에는 붙일 공지가 없어 [공지 링크 복사]가 꺼지고, 기록도 남지 않는다고 알린다.

### 4.2 신청 웹 — 집회 페이지 위쪽에 공지 카드

집회를 불러올 때 공지도 같이 불러온다(두 요청을 같이 띄워 두고 기다린다 — 한 줄씩 기다리면 로딩이 두 배다).
공개된 공지만 온다(RLS 가 거른다 — 앱에서 또 거르지 않는다). **공지를 못 읽어도 신청은 되어야 하므로**
공지 쪽 실패는 삼키고 빈 목록으로 넘어간다.

- 고정 공지 먼저, 그다음 최신순. 3건까지 펼쳐 두고 나머지는 [공지 N개 더 보기].
- 본문이 길면 접어 두고 [더 보기]로 펼친다. 공지가 없으면 카드 자체가 안 나온다.
- `?n=<공지id>` 로 들어오면 그 공지를 맨 위로 올리고 펼쳐 둔다. 지워진 id 면 평소 순서 그대로다.

---

## 5. 브라우저 푸시 알림 — 검토 (아직 안 만듦)

복사·붙여넣기의 한계는 분명하다. **단톡방에 없는 사람에게는 닿지 않는다.**
알림톡 말고 그 자리를 메울 수 있는 무료 수단이 **웹 푸시(Web Push)** 다.

### 5.1 정말 공짜인가 — 그렇다 [높은 확신]

푸시 전달은 브라우저 회사(Chrome·Firefox·Safari)의 푸시 서비스가 공짜로 해 준다. VAPID 키로
서명해서 보내면 되고, 별도 계약·건당 요금이 없다. 보내는 쪽은 Supabase Edge Function 으로 충분하고,
Edge Function 은 무료 요금제에 포함된다(월 호출 한도가 있으나 집회 공지 규모는 한참 아래다).

> 무료 한도 수치는 바뀌므로 여기 적지 않는다. 필요하면 Supabase 요금 페이지를 직접 확인하세요.

### 5.2 그런데 실제로 몇 명에게 닿나 — 이게 진짜 문제다

| 환경 | 되나 | 조건 |
|---|---|---|
| Android Chrome | 된다 | 알림 허용 한 번 |
| 데스크톱 Chrome·Edge·Firefox | 된다 | 알림 허용 한 번 |
| **iPhone / iPad Safari** | **홈 화면에 추가해야 된다** | 신청 웹을 "홈 화면에 추가"한 뒤에야 알림을 켤 수 있다 [높은 확신] |

iPhone 쓰는 신청자에게 "사파리에서 공유 → 홈 화면에 추가 → 다시 열어서 알림 허용"을 시켜야 한다.
**수련회 신청자에게 이걸 시켜서 몇 %가 끝까지 할지는 기대하지 않는 게 맞다.**
안드로이드도 "알림 허용"을 한 번 거절하면 그 뒤로는 앱이 다시 물을 수 없다.

또 브라우저를 바꾸거나 기기를 바꾸면 구독이 끊긴다. 누가 받고 누가 못 받는지를 운영자가 알 수 없다.
**푸시는 "이 공지 봤겠지"를 보장하지 못한다.** 단톡방 공지를 대체할 수 없고, 보조만 된다.

### 5.3 만든다면 이렇게 (설계는 정해 뒀다)

```
[신청자] 신청 완료 화면 → [공지 알림 받기] → 브라우저 알림 허용 → 구독 정보 저장
[운영자] 공지 화면 → [푸시 보내기] → Edge Function(VAPID 서명) → 브라우저 푸시 서비스 → 알림
```

- **표**: `push_subscriptions(gathering_id, endpoint pk, p256dh, auth, created_at)`.
  신청자는 로그인이 없으므로 `registrations` 와 같은 방식으로 **RPC**(`subscribe_push`/`unsubscribe_push`)로만 닿게 하고
  테이블은 운영자 전용으로 막는다. 끊긴 구독(410/404)은 발송 때 지운다.
- **옵트인 자리**: 신청 완료 화면(`DonePage`)이 제일 낫다. 방금 신청을 끝낸 사람이라 동기가 가장 높다.
  집회 페이지 공지 카드에도 작게 하나 둔다. iPhone 이면 "홈 화면에 추가" 안내를 먼저 보여준다.
- **서비스워커**: Flutter 웹이 자기 서비스워커를 이미 등록하므로 우리 것은 **좁은 scope**(`push-sw.js`, scope `push/`)로
  따로 등록한다. 페이지를 제어하지 않아도 푸시는 받는다. [보통 확신 – 실기기 확인 필요]
- **페이로드는 싣지 않는다.** 웹 푸시 페이로드는 암호화(RFC 8291)가 필수라 구현·검증 부담이 크다.
  대신 빈 푸시를 보내고 서비스워커가 공개 공지를 한 번 읽어서 제목을 띄운다 — 어차피 공개 데이터다.
  이러면 서버가 할 일이 **VAPID 서명(ES256 JWT)** 하나로 줄어 Web Crypto 로 끝난다.
- **기록**: `notice_sends.channel` 에 `push` 를 더한다.

### 5.4 권고

**넣는다면 "보조 채널"로만, 그리고 단톡방 복사를 계속 주 경로로 둔다.**
반대로 이런 경우엔 넣을 값이 충분하다 — 신청자 중 단톡방에 없는 사람이 눈에 띄게 많거나,
집회 직전 변경 공지(장소·시간)를 빨리 알려야 하는 일이 반복될 때.

만드는 비용은 돈이 아니라 **검증**이다. 서비스워커·VAPID·iOS 홈 화면 추가는 실기기에서만 확인되고,
테스트로 잡히지 않는다. 그래서 이번에는 설계까지만 해 두고 멈췄다.

---

## 6. 일부러 안 한 것 (`ponytail:`)

- **알림톡** — §1. 유일하게 전화번호로 닿는 길이지만 계약·심사·건당 과금이 붙는다. 단톡방이 있는 한 값을 못 한다.
  나중에 정말 필요해지면 Edge Function 하나(대행사 API 중계 + `notice_sends` 기록)만 더 만들면 되고,
  화면·모델은 그대로 쓴다.
- **카카오톡 공유하기(JS SDK)** — §1. 무료지만 결국 사람이 채팅방을 고르는 건 복사와 같고,
  카카오 개발자 등록이 더 붙고 데스크톱 운영자 앱에서는 안 된다.
- **문자(SMS)** — `sms:` 링크로 문자 앱을 열 수는 있지만 수백 건이면 통신 요금이 붙는다. 무료가 아니다.
- **예약 발송** — 서버 스케줄러(pg_cron 등)가 필요하고, 지금은 공지를 쓰는 사람이 곧 보내는 사람이라 값을 못 한다.
  필요해지면 `notices` 에 `send_at` 을 붙인다.
- **읽음 확인** — 카카오톡이 알려주지 않는다. 링크에 추적 파라미터를 붙이는 방법이 있지만 신청자를 추적하는 건
  이 도구의 성격이 아니다.
- **개인별 문구 치환**("○○님, 302호입니다") — 단톡방에 한 번 붙여넣는 방식에서는 쓸 데가 없다.
- **공지 첨부 이미지** — 포스터 한 장이면 충분하고, 카카오톡에 붙여넣을 땐 어차피 텍스트만 간다.

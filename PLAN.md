# 집회 방배정 프로그램 기획서

Flutter 단일 앱. 호텔 체크인 시스템 참고, 운영자 1명이 쓰는 관리 도구.

## 1. 범위 / 비범위

| 포함 | 제외 (지금은) |
|---|---|
| 방 생성/수정, 참석자 등록(붙여넣기), 방배정, 자동배정, 현황 | 로그인(마지막), 서버/동기화, 다중 사용자 동시편집, 권한 |

- **저장소: 로컬 JSON 파일 1개** (`path_provider` + `dart:convert`). 참석자 수백~천 명 수준은 전부 메모리에 올려도 무방하고, 검색/필터는 `where()` 한 줄이면 끝난다. DB는 필요해질 때 넣는다.
- **상태관리: `ChangeNotifier` + `ValueListenableBuilder`** (Flutter 내장). Riverpod/Bloc은 화면이 4개뿐이라 과함.
- **플랫폼: macOS / Windows 데스크톱 우선**, 동일 코드로 웹도 뜸(웹은 파일 저장 대신 localStorage).

## 2. 데이터 모델

```dart
class Event {          // 집회 1건 = 앱 1개 파일
  String name;
  DateTime startDate;  // 전체 일정 시작
  DateTime endDate;    // 전체 일정 종료
  List<Room> rooms;
  List<Attendee> attendees;
}

class Room {
  String id;
  String roomNo;       // "301" → floor는 roomNo에서 파싱 (int.parse(roomNo) ~/ 100)
  int capacity;        // 수용 가능 인원
  String? gender;      // 'M' | 'F' | null(무관)
  String? note;        // 기타
}

class Attendee {
  String id;
  String name;         // 필수
  String gender;       // 필수 'M' | 'F'
  int age;             // 필수
  String? phone, cell, zone, note;   // 선택 (셀/존)
  String? roomId;      // 배정된 방, null = 미배정
  DateTime checkIn;    // 기본값 = event.startDate
  DateTime checkOut;   // 기본값 = event.endDate
}
```

배정은 별도 테이블 없이 `Attendee.roomId` 한 필드로 끝낸다. 한 사람이 일정 중 방을 옮기는 케이스는 요구사항에 없으므로 만들지 않는다.

## 3. 화면 (하단/좌측 탭 4개)

### 3.1 방 관리
- 목록 + [방 추가] 다이얼로그: 호수, 수용인원, 성별(선택), 기타.
- 여러 방 일괄 생성: `301-310` 같은 범위 입력 → 한 번에 생성. (호수 규칙이 연속적이라 이게 제일 빠름)
- 삭제 시 배정된 인원이 있으면 경고 후 미배정으로 되돌림.

### 3.2 참석자 관리
- **입력 방식: 붙여넣기 텍스트에어리어 하나.** 엑셀에서 여러 컬럼/로우를 복사하면 탭(`\t`) 구분 TSV로 붙는다. `text.split('\n').map((l) => l.split('\t'))` 로 파싱 끝. 파일 업로드(.xlsx)는 라이브러리 의존성이 붙으므로 **나중에**.
  - 상단에 컬럼 순서 지정 드롭다운 (기본: 이름 / 성별 / 나이 / 전화 / 셀 / 존 / 기타)
  - 파싱 후 **미리보기 테이블 → 확인 후 저장**. 오류 행은 빨갛게 표시하고 그 행만 건너뜀.
  - 성별은 `남/여/M/F/m/f` 모두 허용, 나이는 숫자만.
- 목록: 검색창(이름/셀/존/전화 통합 검색) + 정렬.
- 개별 추가/수정/삭제.

### 3.3 방배정
좌: **참석자 필터 패널** / 우: **방 목록 패널**

- 필터: 이름, 셀, 존, 성별, 나이 범위(min~max), 배정여부(미배정만 보기).
- 필터 결과 전체 선택 → 방 하나 클릭 → **[일괄 배정]**. (요구사항: "특정 셀, 나이 등을 검색해서 한번에 특정 방에 적용")
- 체크인/체크아웃: 선택한 인원에 대해 날짜 일괄 지정. 기본값은 전체 일정.
- 방 카드 표시: 호수 + `4/6` + 잔여 막대 + 성별 배지. 한 줄에 10칸씩, 층별로 묶어 **높은 층이 위**.
  상태는 색으로 구분한다: **공실=흰색 / 여유=청록 / 만실=주황 / 초과=빨강**.
  (초과 배정은 막지 않고 색으로 경고만 — 현장에서 필요한 경우가 있음)
  색만으로 판단하지 않도록 타일에는 항상 숫자와 막대를 같이 그린다.
- 정원 계산은 **날짜 겹침 기준**: 부분 참석자가 있으므로 "그 방의 어느 하룻밤이라도 정원을 넘으면 초과". 
  ```dart
  int peakOccupancy(Room r, List<Attendee> all) => 
    eventNights.map((d) => all.where((a) => a.roomId == r.id && !d.isBefore(a.checkIn) && d.isBefore(a.checkOut)).length)
               .fold(0, max);
  ```

### 3.4 자동배정
운영자가 **규칙을 설정**하고 [자동배정 실행] → 미배정 인원만 채운다. 기존 배정은 건드리지 않음.

설정 항목:
- 우대 대상 기준: `나이 >= N` (기본 65) 또는 참석자 `기타사항`에 특정 키워드(예: `강사`) 포함
- 우대 대상 배정 구역: 층 지정(예: 1~2층) 또는 호수 범위 지정(예: 101~215)
- 성별 분리 여부 (기본 ON — 방 성별이 정해졌거나 비어있으면 첫 배정자 성별로 고정)

알고리즘 (그리디, 3단계):
1. 우대 대상부터 처리 → 지정된 층/호수 범위 안의 빈자리에 배정. 나이 많은 순으로 낮은 층 먼저.
2. 나머지를 `(존, 셀)` 그룹으로 묶고, **큰 그룹부터** 처리. 그룹은 남은 자리가 가장 많은 방부터 채우되, **같은 그룹이 이미 있는 방과 호수가 가까운 방**을 우선(`|roomNo - 기준roomNo|` 최소).
3. 남은 인원은 성별 맞는 빈자리 아무 데나.

> `ponytail:` 그리디 + 호수 근접도만 본다. 최적해가 아니고 O(n·m) 스캔이다. 운영자가 결과를 보고 수동으로 고치는 게 전제 — 실제로 불만이 나오면 그때 백트래킹/코스트 함수로 올린다.

실행 결과는 **미리보기(변경될 항목 목록)** 를 먼저 보여주고 [적용] / [취소]. 되돌리기는 미적용으로 대체.

### 3.5 현황보기
한 화면에 4블록:
- **전체 구조**: 배정 화면과 같은 `RoomBoard` (층별 그리드, 공실=흰색 / 여유=청록 / 만실=주황 / 초과=빨강). 타일 클릭 → 해당 방 인원 목록.
- **요약 숫자**: 총원 / 배정완료 / 미배정 / 총 수용인원 / 잔여 좌석.
- **미배정 인원 목록** (클릭 시 배정 화면으로 이동)
- **여유 있는 방 목록**

### 3.6 로그인 (마지막)
단일 관리자 비밀번호 1개를 로컬에 해시 저장. 서버가 없으므로 그 이상은 의미 없음.

## 4. 파일 구조

```
lib/
  main.dart              앱 + 탭 셸
  models.dart            Event/Room/Attendee + toJson/fromJson
  store.dart             ChangeNotifier, 로드/저장, 검색·정원 계산 헬퍼
  auto_assign.dart       자동배정 규칙 + 알고리즘
  theme.dart             디자인 토큰(색·모서리) + 방 상태 판정 + 공용 위젯
  widgets/room_board.dart 방 타일 / 층별 보드 / 범례
  screens/rooms.dart / attendees.dart / assign.dart / status.dart
test/
  auto_assign_test.dart   자동배정·정원계산 검증
  widget_test.dart        화면 렌더 + 보드 레이아웃 검증
```

UI 규칙은 `.claude/skills/room-ui/SKILL.md` 에 따로 정리해 두었다.

의존성: `path_provider` 만. (직렬화 코드젠, DI, 라우터, 상태관리 패키지 전부 불필요)

## 5. 구현 순서

1. `models.dart` + `store.dart` (JSON 저장/로드) — 여기가 되면 나머지는 UI일 뿐
2. 방 관리 화면 (범위 일괄 생성 포함)
3. 참석자 붙여넣기 파싱 + 목록
4. 배정 화면 (필터 → 일괄 배정, 정원 표시)
5. 자동배정 + `auto_assign_test.dart`
6. 현황 화면
7. 로그인

## 6. 나중에 (지금 안 만드는 것)

- .xlsx 파일 업로드 → 붙여넣기로 불편해지면 `excel` 패키지 추가
- 배정표 인쇄/엑셀 내보내기 → 요청 오면 CSV 한 줄로
- 배정 이력/되돌리기(undo) → 자동배정 미리보기로 충분한지 먼저 확인
- SQLite → 참석자 수천 명 넘어가거나 앱이 느려지면
